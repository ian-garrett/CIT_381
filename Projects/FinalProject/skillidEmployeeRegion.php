<?php

include('connectionData.txt');

$conn = mysqli_connect($server, $user, $pass, $dbname, $port)
or die('Error connecting to MySQL server.');

?>

<head>
  <title>Employees who possess Skill</title>
 <link rel="stylesheet" type="text/css" href="style.css">
  </head>
  
  <body bgcolor="white">
  <div id="text">
  
  <hr>
  
  
<?php
  
$state = $_POST['state'];

$state = mysqli_real_escape_string($conn, $state);
// this is a small attempt to avoid SQL injection
// better to use prepared statements

$query = "SELECT
concat(e.firstName,' ', e.lastName) AS 'Employees with Skill', 
e.REGIONS_code AS 'Employee Regional Location',
(s.descript) AS 'Skills'
FROM EMPLOYEES e JOIN EMPLOYEES_have_SKILLS h ON e.ID=h.EMPLOYEES_ID
JOIN SKILLS s ON h.SKILLS_ID=s.ID
WHERE s.ID=";
$query = $query."'".$state."';";

?>

<p>
The query:
<p>
<?php
print $query;
?>

<hr>
<p>
Result of query:
<p>

<?php
$result = mysqli_query($conn, $query)
or die(mysqli_error($conn));

echo "<table width ='100%' border='1'>
        <tr>
        <th>Employee</th>
        <th>Region</th>
        </tr>";

        //  $row = mysqli_fetch_row($query);
                while($row = mysqli_fetch_array($result, MYSQLI_BOTH)){
                echo "<tr>";
                echo "<td>" . $row['Employees with Skill'] . "</td>";
                echo "<td>" . $row['Employee Regional Location'] . "</td>";

                
               
        //  $row = mysqli_fetch_row($query);
                        }
        echo "</table>";

mysqli_free_result($result);

mysqli_close($conn);

?>

<p>
<hr>

<p>
 <span><button><a href="index.html">Home</a></button> <button><a href="skillidEmployeeRegion.html">Reset</a></button></span>	 
 
</body>
</html>
	  